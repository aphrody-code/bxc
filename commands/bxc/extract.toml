description = "Extract structured data from a URL using local AI"

prompt = """
I will extract structured data from the URL using the Bxc engine and local AI.
Usage: /bxc:extract <url> <schema>

!{bxc-mcp call bxc_extract_structured '{"url": "{{args}}", "schema": "JSON"}'}

(Note: Ensure bxc-mcp is installed and configured in your PATH)
"""
