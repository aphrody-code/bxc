#!/usr/bin/env bun
// SPDX-License-Identifier: Apache-2.0
// MOVED: this full-account X→yoyo.sqlite mapper now lives in the yoyo monorepo.
// Canonical: yoyo/packages/x-sync/src/map.ts  (run: `cd yoyo && bun run sync`).
console.error(
  "x-yoyo-map relocated → yoyo/packages/x-sync (run: cd yoyo && bun run sync)",
);
process.exit(2);
