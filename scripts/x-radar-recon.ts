#!/usr/bin/env bun
// SPDX-License-Identifier: Apache-2.0
// MOVED: X Radar recon now lives in the yoyo monorepo (portable, @aphrody/x).
// Canonical: yoyo/scripts/recon/x-radar-recon.ts  (run: `cd yoyo && bun run radar:recon`).
console.error(
  "x-radar-recon relocated → yoyo/scripts/recon (run: cd yoyo && bun run radar:recon)",
);
process.exit(2);
